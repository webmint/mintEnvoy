{
  "schema_version": "1.0",
  "handoff_kind": "plan",
  "plan_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/005-tab-bar-visual-fidelity/plan.md",
  "plan_completed_at": "2026-06-26T05:59:41Z",
  "provenance": {
    "upstream_handoff_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/005-tab-bar-visual-fidelity/handoff.json",
    "upstream_handoff_kind": "specify",
    "spec_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/005-tab-bar-visual-fidelity/spec.md"
  },
  "breakdown_seeds": {
    "layer_map": [
      {
        "layer": "Renderer \u00b7 molecules (Tabs contract + render)",
        "what": "Add optional `method?: string` + `dirty?: boolean` to `TabDescriptor`; render a leading `.method` chip before the label in BOTH branches gated on `method !== undefined`; in the closable branch render dirty-dot-XOR-close; add `tabs__tab-wrapper--active` modifier to the wrapper; import the Icon atom (`x`)",
        "files": "src/renderer/src/components/molecules/Tabs.tsx"
      },
      {
        "layer": "Renderer \u00b7 molecules (Tabs styles)",
        "what": "New active `::before`(1.5px --accent)/`::after`(1px --bg mask) + `--bg` surface on `.tabbar .tabs__tab-wrapper--active`; `.tabs__tab-dirty` 7px --text-faint dot; rewrite `.tabs__tab-close` to always-visible 16px grid-centered (radius 3px, --text-faint, hover --bg-active); `.tabbar`-scoped geometry (gap 8px, padding 0 10px 0 12px, border-right 1px --border on the wrapper, label `flex:1` ellipsis); override `.tabs.tabbar { overflow: visible }`",
        "files": "src/renderer/src/components/molecules/Tabs.css"
      },
      {
        "layer": "Renderer \u00b7 organisms (TabBar)",
        "what": "`toDescriptor` adds `method: tab.spec.method` + `dirty: tab.dirty` and REMOVES `badge: tab.dirty ? '\u25cf'`; `deriveLabel` unchanged (AC-5); actions slot \u2192 `+` button (Icon `plus`) + `flex:1` spacer + static \"More tabs\" chevron (Icon `chevronDown`, `TODO(overflow)` marker)",
        "files": "src/renderer/src/components/organisms/TabBar.tsx"
      },
      {
        "layer": "Renderer \u00b7 organisms (TabBar styles)",
        "what": "`.tabbar` \u2192 --bg-sunken, height 36px, border-bottom 1px --border, padding-right 8px; `.tabbar__new` + chevron as grid-centered icon buttons; DELETE the `.tabbar .tabs__badge` dirty-dot block",
        "files": "src/renderer/src/components/organisms/TabBar.css"
      },
      {
        "layer": "Renderer \u00b7 styles (tokens + global method)",
        "what": "Add base `.method { font-family: var(--font-mono); \u2026 }` (AC-8 \u2014 mstyle-invariant only); add `--m-head` light+dark token (AC-1); add base `.method.HEAD` + `[data-mstyle='soft'] .method.HEAD` color rules (AC-7/AC-19)",
        "files": "src/renderer/styles/tokens.css"
      },
      {
        "layer": "Renderer \u00b7 organisms (Shell border de-dup)",
        "what": "Remove `border-bottom` from `.shell__tabs` (Shell.css:130) so the single remaining border is `.tabbar`'s \u2014 required for the active `::after` mask to read (AC-17)",
        "files": "src/renderer/src/components/organisms/Shell.css"
      },
      {
        "layer": "Test harness (CT styling context \u2014 Decision (g))",
        "what": "Import `tokens.css` globally into the CT mount root so every CT page resolves real token values + `.method` rules; the fidelity suite sets `data-mstyle='soft'` per-test on the document root (the prerequisite for AC-18/AC-19/AC-21 computed-style resolution)",
        "files": "playwright/index.tsx"
      },
      {
        "layer": "Docs \u00b7 002 lineage",
        "what": "Append `### Extension: feature 005-tab-bar-visual-fidelity` under \u00a710 (new method/dirty fields, dirty-XOR-close model, byte-identical non-closable path, global-`.method` departure, Q-3 a11y tradeoff) (AC-23)",
        "files": "specs/002-tabs-primitive/spec.md"
      },
      {
        "layer": "Tests",
        "what": "Tabs.test (byte-identical non-closable, dirty-XOR-close, method chip, keyboard Delete closes dirty); TabBar.test (migrate AC-26 `'\u25cf'` \u2192 `.tabs__tab-dirty`/`.tabs__tab-close`, dot-click\u2192onClose, method chip, static chevron); Tabs.ct (real-browser computed-style EXACT on geometry/typography/color + active `::before`/`::after` + thresholded screenshot diff, under per-test `data-mstyle='soft'`); Tabs.stories dirty fixture",
        "files": "src/renderer/src/components/molecules/__tests__/{Tabs.test.tsx,Tabs.ct.tsx,Tabs.stories.tsx}, src/renderer/src/components/organisms/__tests__/TabBar.test.tsx"
      }
    ],
    "key_design_decisions": [
      {
        "decision": "(a) Active-tab accent treatment",
        "chosen_approach": "Top `::before` 1.5px --accent + bottom `::after` 1px --bg mask over a --bg surface, placed on a new `tabs__tab-wrapper--active` modifier; neutralize the inherited box-shadow underline + --accent-soft inside `.tabbar`",
        "why": "DEPARTURE: replaces the codebase's established box-shadow-underline + accent-soft active treatment (Tabs.css:142) \u2014 needed because the reference (design/styles.css:710-727) connects the active tab to the pane via a top accent + bottom mask, which box-shadow can't reproduce. Scoped to `.tabbar` as CSS-isolation hygiene so the rules never leak to any other global `.tabs` consumer (AC-22). Wrapper modifier (not `:has()`) so the accent spans the full tab cell incl. the close, and stays testable. Selector `.tabbar .tabs__tab-wrapper--active` (0,2,0) beats global `.tabs__tab--active` (0,1,0) \u2014 no source-order dependence",
        "alternatives_rejected": "`:has(.tabs__tab--active)` on the wrapper: avoidable Chromium-version coupling when we already edit Tabs.tsx; `::before` on the button: spans only the button, not the close cell"
      },
      {
        "decision": "(b) `.tabs overflow:hidden` vs the `bottom:-1px` mask",
        "chosen_approach": "Override `.tabs.tabbar { overflow: visible }` (specificity 0,2,0, leave global `.tabs{overflow:hidden}` intact); remove the duplicate Shell.css:130 border",
        "why": "The `::after` mask sits 1px below the tab to paint over the strip's bottom border \u2014 `overflow:hidden` would clip it. Scoping `overflow:visible` to `.tabbar` keeps the global `.tabs` clip behavior intact for any other consumer. The Shell border de-dup is LOAD-BEARING, not cosmetic: with two borders the mask covers only `.tabbar`'s, leaving Shell's visible and breaking the connect (AC-17). Q-4 runtime probe confirms no `::before`/chevron clipping at `/verify`",
        "alternatives_rejected": "Global `overflow:visible` removal: changes the global `.tabs` clip behavior unnecessarily; keep-and-re-approach the mask: diverges from the enumerated reference geometry AC-11 asserts"
      },
      {
        "decision": "(c) Method chip via GLOBAL `.method`/`.{METHOD}` classes inside the BEM Tabs primitive",
        "chosen_approach": "`<span className={cx('method', knownMethodClass)}>` \u2014 reuse tokens.css's existing `.method` color system; narrow via a `KNOWN_METHODS` const",
        "why": "DEPARTURE: Tabs is BEM-only (`tabs__*`); this injects the global `.method` namespace \u2014 needed by \"search before building\" (\u00a73.6): tokens.css already ships `.method.GET\u2026OPTIONS` \u00d7 6 mstyle variants and Shell drives `data-mstyle` at app runtime; a BEM-local re-implementation would duplicate ~40 color rules and visually diverge from the sidebar/request-bar method rendering. Recorded in the 002 lineage (AC-23, \u00a72.3). States: known-method\u2192AC-9/AC-19 (colored), unknown-method\u2192AC-10 (uncolored, inherits), method `undefined`\u2192AC-2 (no chip, byte-identical)",
        "alternatives_rejected": "BEM-local `tabs__tab-method--get\u2026`: DRY violation + divergence from the app-wide method palette"
      },
      {
        "decision": "(d) `--m-head` color value",
        "chosen_approach": "Light `#ec4899`, dark `#f472b6` (Tailwind pink-500/400); base `.method.HEAD{color:var(--m-head)}` + `[data-mstyle='soft'] .method.HEAD{background:color-mix(\u202616%); color:var(--m-head)}`",
        "why": "Q-1 resolved. Pink is the cleanest open hue \u2014 distinct from GET sky/POST green/PUT amber/PATCH purple/DELETE red/OPTIONS slate and from the emerald accent \u2014 and stays palette-consistent (every existing token is a Tailwind-500 light / -400 dark pair). Base rule satisfies AC-7 grep + gives a colored fallback under any mstyle; soft rule matches the six siblings at the default mstyle (AC-19). Non-soft variants for HEAD are out (gold-plating; AC-19 is \"at default\")",
        "alternatives_rejected": "Indigo `#6366f1`: sits between GET sky and PATCH purple (\u226440\u00b0 hue gap) \u2014 confusable; teal `#14b8a6`: collides with the emerald accent"
      },
      {
        "decision": "(e) Screenshot-diff threshold",
        "chosen_approach": "Playwright `toHaveScreenshot({ threshold: 0.2, maxDiffPixelRatio: 0.01, animations: 'disabled' })`, pinned device scale",
        "why": "Q-2 resolved. `threshold:0.2` (per-pixel YIQ, the Playwright default) absorbs sub-pixel anti-aliasing without masking a token/geometry regression; `maxDiffPixelRatio:0.01` (\u22641% of pixels) bounds the total drift; `animations:'disabled'` removes transition flake. Verification rests on the EXACT computed-style assertions (AC-18) with the diff as the supplementary gate (AC-21) \u2014 not eyeballing",
        "alternatives_rejected": "`threshold:0` / `maxDiffPixels:0`: anti-aliasing flake \u2192 false failures; high `maxDiffPixelRatio` (>0.05): masks real geometry drift"
      },
      {
        "decision": "(f) Dirty-dot-XOR-close render structure",
        "chosen_approach": "In the closable wrapper, after the `role=\"tab\"` button render XOR: `dirty` \u2192 `<span className=\"tabs__tab-dirty\" onClick={stopProp\u2192onClose}>` (7px --text-faint, no role, not focusable, not in `buttonRefs`); clean \u2192 `<button type=\"button\" tabIndex={-1} aria-label={`Close ${label}`} className=\"tabs__tab-close\"><Icon name=\"x\" size={11}/></button>`. Tab button keeps the sole roving stop; Delete/Backspace path unchanged",
        "why": "Span (not button) for the dirty marker matches the reference's non-interactive `.tab-dirty` span (Q-3) and sidesteps the \"button without discernible text\" axe error while staying pointer-clickable; neither marker enters `buttonRefs` or carries `tabIndex\u22650`, so exactly one roving stop survives (AC-3); the existing `handleKeyDown` Delete/Backspace branch already fires `onClose` independent of which marker renders, so dirty tabs stay keyboard-closable (AC-6); the marker is a sibling of the label span, never replacing it (AC-4). States: dirty\u2192AC-12, clean\u2192AC-13, non-closable\u2192AC-2 (no marker node). Q-3 no-aria-label tradeoff documented in the 002 lineage",
        "alternatives_rejected": "Dirty marker as `<button tabIndex={-1}>` with no name: axe 4.1.2 failure; adding it to `buttonRefs`: breaks the single-roving-stop invariant (AC-3)"
      },
      {
        "decision": "(g) CT fidelity-harness styling context",
        "chosen_approach": "Import `tokens.css` GLOBALLY into the CT mount root `playwright/index.tsx` (`import '../src/renderer/styles/tokens.css'`); establish `data-mstyle='soft'` on `document.documentElement` PER-TEST, scoped to the fidelity suite in `Tabs.ct.tsx` (a fidelity-`describe` `beforeEach`/`page.evaluate`, or a `hooksConfig`-gated `beforeMount`) \u2014 NOT a global unconditional `data-mstyle`",
        "why": "`tokens.css` is the CT analog of `main.tsx`'s global token sheet \u2014 it belongs at the mount root so every `var(--accent/--bg/--m-head)` and the `.method` rules resolve to real values (fixes grill F-001); index.tsx self-documents as the place for \"global providers.\" `data-mstyle` is Shell's per-render app state, not a static foundation: only the `.method` chip (Tabs alone) consumes it and only the fidelity suite asserts the soft variant, so scoping it per-test confines blast radius and keeps every other CT suite's styling context unchanged (fixes grill F-002 minimally). The harness reproduces the attribute Shell would set at runtime \u2014 in-scope for AC-18/AC-19/AC-21",
        "alternatives_rejected": "(i) `data-mstyle` set globally in index.tsx: changes the styling context for EVERY CT suite (Dropdown/Modal/Toast/\u2026), needlessly risking their baselines for a Tabs-only need; (ii) add a bare `.method{display;min-width;padding;height}` box rule to tokens.css: WORSE \u2014 mutates the REAL app's CSS to satisfy a test; the app deliberately gates `.method` box geometry on `data-mstyle` (the base `.method{font-family}` rule is fine only because font-family is mstyle-invariant; box geometry is not); (iii) import tokens.css per-test only: a CSS import is page-global once loaded, and the token sheet is a genuine runtime foundation \u2014 the mount root is its correct home"
      }
    ],
    "file_impact": [
      {
        "file": "src/renderer/src/components/molecules/Tabs.tsx",
        "action": "Modify",
        "what_changes": "Add optional `method?`/`dirty?` to `TabDescriptor`; leading `.method` chip (narrowed via `KNOWN_METHODS`) before the label in both branches; dirty-dot-XOR-close in the closable branch; `tabs__tab-wrapper--active` modifier; import Icon `x`; JSDoc on the new fields (AC-24)"
      },
      {
        "file": "src/renderer/src/components/molecules/Tabs.css",
        "action": "Modify",
        "what_changes": "Active `::before`/`::after`+`--bg` on `.tabbar .tabs__tab-wrapper--active`; `.tabs__tab-dirty`; rewrite `.tabs__tab-close` always-visible 16px; `.tabbar`-scoped geometry (gap 8px, padding 0 10px 0 12px, wrapper border-right, label flex:1 ellipsis); `.tabs.tabbar{overflow:visible}`"
      },
      {
        "file": "src/renderer/src/components/organisms/TabBar.tsx",
        "action": "Modify",
        "what_changes": "`toDescriptor` adds `method`/`dirty`, removes `badge:'\u25cf'`; actions slot \u2192 + / spacer / static chevron with `TODO(overflow)`"
      },
      {
        "file": "src/renderer/src/components/organisms/TabBar.css",
        "action": "Modify",
        "what_changes": "`.tabbar` --bg-sunken/36px/border-bottom/padding-right 8px; `.tabbar__new` + chevron icon buttons; delete `.tabbar .tabs__badge`"
      },
      {
        "file": "src/renderer/styles/tokens.css",
        "action": "Modify",
        "what_changes": "base `.method{font-family:var(--font-mono)}` (mstyle-invariant only \u2014 NO box geometry); `--m-head` light+dark; `.method.HEAD` + soft-mstyle `.method.HEAD`"
      },
      {
        "file": "src/renderer/src/components/organisms/Shell.css",
        "action": "Modify",
        "what_changes": "Remove `.shell__tabs` `border-bottom` (line 130)"
      },
      {
        "file": "**playwright/index.tsx**",
        "action": "**Modify (grill-revision)**",
        "what_changes": "Add `import '../src/renderer/styles/tokens.css'` so the CT mount root reproduces the app's global design-token context (token values + `.method`/`[data-mstyle]` rules) for every CT page \u2014 the harness prerequisite for AC-18/AC-19/AC-21 computed-style resolution"
      },
      {
        "file": "specs/002-tabs-primitive/spec.md",
        "action": "Modify",
        "what_changes": "Append `### Extension: feature 005-tab-bar-visual-fidelity` under \u00a710 (AC-23)"
      },
      {
        "file": "src/renderer/src/components/molecules/__tests__/Tabs.test.tsx",
        "action": "Modify",
        "what_changes": "byte-identical non-closable regression, dirty-XOR-close, method chip, keyboard-Delete closes dirty"
      },
      {
        "file": "src/renderer/src/components/molecules/__tests__/Tabs.ct.tsx",
        "action": "Modify",
        "what_changes": "real-browser computed-style EXACT (geometry/typography/color + active ::before/::after) + thresholded screenshot diff; **PLUS (grill-revision): before the fidelity assertions, set `data-mstyle='soft'` on `document.documentElement` scoped to the fidelity suite (fidelity-`describe` `beforeEach`/`page.evaluate` or `hooksConfig`-gated `beforeMount`), and assert token-RESOLVED values \u2014 do NOT set `data-mstyle` globally in index.tsx**"
      },
      {
        "file": "src/renderer/src/components/molecules/__tests__/Tabs.stories.tsx",
        "action": "Modify",
        "what_changes": "dirty fixture (dot click \u2192 onClose; dot not a tab stop; active ::before; method chip)"
      },
      {
        "file": "src/renderer/src/components/organisms/__tests__/TabBar.test.tsx",
        "action": "Modify",
        "what_changes": "migrate AC-26 `'\u25cf'` assertions \u2192 `.tabs__tab-dirty`/`.tabs__tab-close`; dot-click \u2192 close; method chip; static chevron"
      }
    ],
    "doc_impact": [
      {
        "doc_file": "specs/002-tabs-primitive/spec.md \u00a710",
        "action": "Update",
        "what_changes": "Contract-lineage block for the 005 extension (covered as AC-23 \u2014 the canonical lineage record; counted in File Impact above)"
      },
      {
        "doc_file": "docs/renderer/*/index.md (Tabs/TabBar concern)",
        "action": "Update (at `/finalize`)",
        "what_changes": "Note the method chip + dirty-XOR-close + active-accent fidelity additions if the concern doc enumerates Tabs behavior \u2014 tech-writer surgical update, deferred to `/finalize`"
      }
    ],
    "risks": [
      {
        "risk": "Active-CSS rewrite + `overflow:visible` override regresses any global `.tabs` consumer or clips the new accent/chevron",
        "likelihood": "Med",
        "impact": "Med",
        "mitigation": "Scope every new active/geometry/overflow rule to `.tabbar` (compound `.tabs.tabbar`, `.tabbar .tabs__tab-wrapper--active`); keep a closable=false byte-identical regression test (AC-2) \u2014 this test, NOT any mounted consumer, is the proof the non-closable path is unchanged; confirm via the `/verify` runtime design-auditor probe that `::before`/`::after`/chevron are not clipped (Q-4)"
      },
      {
        "risk": "jsdom cannot resolve `::before`/`::after`; a stale CT build cache masks clean source as broken",
        "likelihood": "Med",
        "impact": "Med",
        "mitigation": "Author the EXACT computed-style + pseudo-element assertions in Playwright CT (AC-18); on a build/test error naming a clean file, clear `playwright/.cache`, `node_modules/.vite`, `dist` and re-run before editing source (2026-06-24 memory lesson)"
      },
      {
        "risk": "Dropping `badge:'\u25cf'` + deleting `.tabbar .tabs__badge` leaves the existing TabBar.test AC-26 `'\u25cf'` assertions failing",
        "likelihood": "Med",
        "impact": "Low",
        "mitigation": "Migrate the AC-26 dirty-marker assertions to `.tabs__tab-dirty`/`.tabs__tab-close` + dot-click\u2192onClose in the same change; re-assert the label text is still present (AC-4)"
      },
      {
        "risk": "New `Icon` import in Tabs adds a molecule\u2192atom edge; method chip could leak the global `.method` namespace into non-tabbar consumers",
        "likelihood": "Low",
        "impact": "Low",
        "mitigation": "Molecule\u2192atom is the allowed downward direction (\u00a72.2); the chip renders only when `tab.method` is set and a non-method consumer passes none, so byte-identity holds (AC-2); record the global-`.method` departure in the 002 lineage"
      },
      {
        "risk": "Importing `tokens.css` globally into `playwright/index.tsx` shifts the styling context of existing CT suites (Dropdown/Modal/Toast) \u2014 currently rendering in CT against `var()` defensive fallbacks, not real tokens \u2014 perturbing layout-sensitive assertions or any screenshot baseline",
        "likelihood": "Med",
        "impact": "Low",
        "mitigation": "Existing CT assertions read component-local `animation-name` + boundingBox with \u00b11px tolerance (token-robust); after the import, run the full CT suite and regenerate/verify any affected baseline as a one-time isolated step BEFORE adding the Tabs fidelity assertions \u2014 do not co-mingle baseline regen with source edits"
      },
      {
        "risk": "First-run CT fidelity baselines (computed-style + screenshot) are authored against the new harness, so there is no prior baseline to diff \u2014 a wrong harness setup could bake an off-palette baseline",
        "likelihood": "Med",
        "impact": "Med",
        "mitigation": "Author the EXACT computed-style assertions (AC-18) as the primary gate (they fail loudly on unresolved tokens \u2014 a `var()`-unresolved color is not the target literal); treat `toHaveScreenshot` as the supplementary gate only (decision (e)); manually confirm the first screenshot baseline renders the soft-mstyle palette before committing it"
      }
    ],
    "specialist_consultation": [],
    "dependencies": [
      "None \u2014 no packages to install, no services to configure, no environment variables. All assets are in-stack: the Icon atom glyphs (`x`/`plus`/`chevronDown`, atoms/icons.ts), the tokens.css `.method` palette, and Playwright CT (`snapshotDir: ./__snapshots__` already configured; the mount root `playwright/index.tsx` is modified, not added)."
    ]
  }
}
