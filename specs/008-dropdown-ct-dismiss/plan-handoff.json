{
  "schema_version": "1.0",
  "handoff_kind": "plan",
  "plan_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/008-dropdown-ct-dismiss/plan.md",
  "plan_completed_at": "2026-06-27T18:29:57Z",
  "provenance": {
    "upstream_handoff_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/008-dropdown-ct-dismiss/handoff.json",
    "upstream_handoff_kind": "specify",
    "spec_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/008-dropdown-ct-dismiss/spec.md"
  },
  "breakdown_seeds": {
    "layer_map": [
      {
        "layer": "Renderer \u00b7 component-test (Playwright CT)",
        "what": "Before each corner click, gate the single outside click on an animation-completion await + a `setTimeout(0)` macrotask-boundary floor; strict assertions unchanged",
        "files": "`src/renderer/src/components/molecules/__tests__/Dropdown.ct.tsx` (two cases \u2014 dismiss at `:185`, outside click at `:198`; focus-return at `:236`, outside click at `:253`)"
      },
      {
        "layer": "Renderer \u00b7 CT fixture",
        "what": "No change \u2014 preferred design needs no fixture affordance",
        "files": "`src/renderer/src/components/molecules/__tests__/Dropdown.stories.tsx` (untouched)"
      }
    ],
    "key_design_decisions": [
      {
        "decision": "Readiness-signal API",
        "chosen_approach": "`await menu.evaluate(el => Promise.all(el.getAnimations().map(a => a.finished)))` **then** one in-page `setTimeout(0)` macrotask-boundary yield, before each corner click",
        "why": "Deterministic in CT (140ms entry animation \u226b Radix's `setTimeout(0)`, so the listener is armed) AND motion-independent (the 0-delay floor crosses the macrotask boundary after Radix's earlier-queued `setTimeout(0)` even when no animation exists). The `0` is an event-loop ordering primitive, not a hardware-racing duration \u2192 outside \u00a76's magic-number rationale. Satisfies AC-3/4/5 and \u00a79 Risk-1.",
        "alternatives_rejected": "(A) animation-await alone \u2014 `Promise.all([])` resolves immediately under zero-animation \u2192 \u00a79 Risk-1 resurfaces. (B) `waitForFunction` opacity-poll \u2014 same animation dependency, brittle computed-style read. (C) single rAF/microtask \u2014 does not order after a `setTimeout(0)` macrotask \u2192 false determinism. `waitForTimeout(150)` \u2014 \u00a76 OOS bare magic-number."
      },
      {
        "decision": "Action fidelity",
        "chosen_approach": "Keep a **single** outside corner click per test",
        "why": "AC-1/AC-3/AC-4 model \"a user clicks outside\" once; gate the click, do not repeat it",
        "alternatives_rejected": "`expect(async () => { click; assert }).toPass()` \u2014 re-dispatches the click, diverging from the single-click interaction the ACs model"
      },
      {
        "decision": "Scope confinement",
        "chosen_approach": "Edit only the two failing cases; fixture + production Dropdown untouched",
        "why": "\u00a76 exclusions + constitution \u00a76.1; production path proven correct by Modal CT",
        "alternatives_rejected": "Touching `Dropdown.tsx`/Radix (\u00a76); hardening Modal/nested-overlays CT (\u00a76)"
      },
      {
        "decision": "Reuse",
        "chosen_approach": "Conceptual reuse of Modal's \"armed-before-dismissal\" convention via the readiness wait",
        "why": "Constitution \u00a76.3 \u2014 Modal's literal open-at-mount can't transfer (Dropdown opens via trigger), so reuse the intent",
        "alternatives_rejected": "A new shared test helper \u2014 no third caller (DRY rule-of-three unmet)"
      }
    ],
    "file_impact": [
      {
        "file": "src/renderer/src/components/molecules/__tests__/Dropdown.ct.tsx",
        "action": "Modify",
        "what_changes": "In the two failing cases, insert the readiness gate (animation-completion await + `setTimeout(0)` floor) immediately before the corner clicks at `:198` and `:253`. Add a one-line comment on the `setTimeout(0)` line explaining it is a macrotask-boundary readiness floor (so the `/implement` review panel does not read it as a banned fixed-delay sleep). Strict assertions at `:200`, `:256`, `:259` unchanged."
      }
    ],
    "doc_impact": [],
    "risks": [
      {
        "risk": "Reviewer mistakes the `setTimeout(0)` floor for a banned fixed-delay sleep (\u00a76)",
        "likelihood": "Low",
        "impact": "Low",
        "mitigation": "Breakdown mandates an explanatory comment on that line and pairs it with the named animation-completion await so intent reads as readiness, not duration."
      },
      {
        "risk": "One macrotask turn assumed sufficient but the arm needs more",
        "likelihood": "Low",
        "impact": "Med",
        "mitigation": "The animation-completion await already crosses far past one turn in CT; the `setTimeout(0)` floor is only the zero-animation backstop; AC-5's repeated-run check catches residual flake."
      },
      {
        "risk": "Focus-return case: corner click blurs the trigger before arm",
        "likelihood": "Low",
        "impact": "Low",
        "mitigation": "Assert `toBeFocused()` once, after the readiness-gated single click; Radix `FocusScope` restores focus to the trigger on dismiss."
      },
      {
        "risk": "Breakdown drifts to a `toPass` retry loop",
        "likelihood": "Low",
        "impact": "Low",
        "mitigation": "Decision 2 fixes a single click; the breakdown task flags this explicitly."
      },
      {
        "risk": "Production Dropdown dismissal regresses in the future, masked by the wait (\u00a77 not-break)",
        "likelihood": "Low",
        "impact": "Low",
        "mitigation": "Assertions stay strict (`not.toBeVisible` / `toBeFocused`), so a real dismissal break still fails after the wait."
      }
    ],
    "specialist_consultation": [],
    "dependencies": [
      "None \u2014 no packages to install, no services to configure, no environment variables. Uses the existing `@playwright/experimental-ct-react` stack and the DOM `Element.getAnimations()` API."
    ]
  }
}
