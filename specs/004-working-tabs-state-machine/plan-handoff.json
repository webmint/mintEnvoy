{
  "schema_version": "1.0",
  "handoff_kind": "plan",
  "plan_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/004-working-tabs-state-machine/plan.md",
  "plan_completed_at": "2026-06-24T21:15:40Z",
  "provenance": {
    "upstream_handoff_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/004-working-tabs-state-machine/handoff.json",
    "upstream_handoff_kind": "specify",
    "spec_path": "/Users/mykolakudlyk/Projects/private/mintEnvoy/specs/004-working-tabs-state-machine/spec.md"
  },
  "breakdown_seeds": {
    "layer_map": [
      {
        "layer": "lib (model)",
        "what": "`RequestSpec` + `Row` + `Auth` discriminated union (`{none}`\\",
        "files": "`{bearer,token}`), `isBearerAuth` type guard, `makeBlankRequest()` seed factory (GET / empty url+name+body / Accept:application/json header / bearer `{{apiKey}}`); plain-serializable, no auth\u2192headers mirroring"
      },
      {
        "layer": "lib (state)",
        "what": "`tabsStore` zustand slice `{ tabs: Tab[]; activeTabId }`; `Tab` carries a `collectionRequestId: string \\",
        "files": "null`; actions `openFromCollection(input: OpenFromCollectionInput)` (two-leg dedupe: `collectionRequestId` first, then verbatim non-empty url), `newBlank`, `close` (never-zero + right-then-left neighbor for the active-tab branch; `activeTabId` UNCHANGED when a non-active tab closes), `selectActive`, `markClean`; tab-id via `crypto.randomUUID()`"
      },
      {
        "layer": "molecules",
        "what": "Tabs primitive extension \u2014 opt-in `closable`/`onClose` props (default-off); sibling `<button tabIndex={-1}>` \u2715 per tab + Delete/Backspace close on focused tab; `onClose` signal-only; roving-focus restored on re-render",
        "files": "`src/renderer/src/components/molecules/Tabs.tsx`, `Tabs.css` (modify)"
      },
      {
        "layer": "organisms",
        "what": "`TabBar` strip \u2014 maps `tabsStore.tabs`\u2192`TabDescriptor[]` (label precedence name\u2192method+url\u2192`Untitled` verbatim; dirty marker via badge slot; `+` via actions slot); wires `selectActive`/`close`/`newBlank`; tokens-bound semantic classes",
        "files": "`src/renderer/src/components/organisms/TabBar.tsx`, `TabBar.css` (new)"
      },
      {
        "layer": "organisms (wiring)",
        "what": "App injects `<Shell tabs={<TabBar />} />` at the composition root, replacing the test-fixture feeder. Shell stays slot-agnostic \u2014 it does NOT import TabBar (no organism\u2192sibling-organism coupling, per Risk 2)",
        "files": "`src/renderer/src/App.tsx` (modify) \u2014 Shell.tsx itself is unchanged"
      },
      {
        "layer": "support (demo)",
        "what": "Register a `closable` Tabs variant (and/or TabBar) in dev-only gallery for manual fidelity check vs `design/reference.html`",
        "files": "`src/renderer/src/components/PrimitivesDemo.tsx` (modify)"
      },
      {
        "layer": "test",
        "what": "Slice unit suite (every lifecycle action + dirty/markClean + invariants + Q-2 serialization contract); TabBar render/select/close; Tabs `closable=true` a11y (axe) + `closable=false` byte-identical regression; CT for keyboard/focus",
        "files": "`src/renderer/src/lib/__tests__/tabsStore.test.ts`, `.../organisms/__tests__/TabBar.test.tsx`, `.../molecules/__tests__/Tabs.test.tsx`, `.../molecules/__tests__/Tabs.ct.tsx` (new/extend)"
      }
    ],
    "key_design_decisions": [
      {
        "decision": "Slice shape",
        "chosen_approach": "Flat `{ tabs: Tab[]; activeTabId }`, array order IS tab order, spec embedded in Tab (Option A)",
        "why": "Pre-settled in /specify; mirrors `settingsStore`/`toastStore` convention (\u00a77, \u00a73.6 favors flat over normalized). Never-zero invariant means no empty state (AC-17), so no 0-tab branch",
        "alternatives_rejected": "Normalized `{ byId, order }` map \u2014 KISS violation, no AC needs O(1) id lookup at this scale"
      },
      {
        "decision": "Where close lives",
        "chosen_approach": "Inside Tabs primitive as opt-in `closable`/`onClose`, default-off; \u2715 is sibling `<button tabIndex={-1}>`, keyboard = Delete/Backspace on focused tab",
        "why": "**DEPARTURE** \u2014 the 002 contract was selection-only; gaining interactive close is a real contract change (record in 002 lineage per AC-29), backward-compatible by construction (default-off, byte-identical 002 path proven by AC-11/AC-12 regression). Exercises AC-22",
        "alternatives_rejected": "TabBar rolling its own \u2715 outside the primitive \u2014 would duplicate roving-tabindex a11y the primitive owns (\u00a76.3)"
      },
      {
        "decision": "onClose contract",
        "chosen_approach": "Signal-only: emits tab id on click/Delete/Backspace, mutates no list; store owns lifecycle; primitive's only post-close job is roving-focus integrity on next render",
        "why": "Keeps the primitive controlled + stateless (matches 002 `onChange`); store is sole owner of tab mutation (\u00a74). Exercises AC-22/AC-23",
        "alternatives_rejected": "Primitive self-removing the tab \u2014 splits lifecycle ownership across primitive + store, breaks SSOT"
      },
      {
        "decision": "Dirty-close policy",
        "chosen_approach": "Silent unconditional drop; clean and dirty close through the SAME path; `dirty` stays an exposed selector; only close branch is the structural never-zero spawn",
        "why": "Pre-settled (\u00a73, AC-19). One close path = \u00a73.6 KISS; no confirm dialog in T4 (trigger sites that gate on dirty are \u00a76 OOS)",
        "alternatives_rejected": "Confirm-on-dirty-close \u2014 depends on a UX prompt \u00a76 defers; over-solve"
      },
      {
        "decision": "RequestSpec serializability (Q-2)",
        "chosen_approach": "Plain object, no class/Symbol/function on the data shape; pin a contract test asserting `JSON.parse(JSON.stringify(spec))` deep-equals `spec`",
        "why": "Q-2 resolved direction; de-risks the OOS persistence task's cross-task contract now (Risk 2) at near-zero cost; actions live on the store wrapper, never on the data",
        "alternatives_rejected": "Class-based RequestSpec with methods \u2014 breaks JSON round-trip, forces persistence rework"
      },
      {
        "decision": "TabBar\u2194store wiring",
        "chosen_approach": "Per-field selectors: subscribe to `tabs` and `activeTabId` separately; pull actions as stable references; no whole-store read",
        "why": "\u00a74 \"prefer zustand selectors\"; avoids re-render on unrelated changes; matches 003 Shell pattern. Exercises AC-24\u2013AC-27",
        "alternatives_rejected": "Whole-store `useTabsStore()` read \u2014 re-renders on any field change, violates selector-preference"
      },
      {
        "decision": "Tab-id generation",
        "chosen_approach": "`crypto.randomUUID()` at tab-create time (newBlank / openFromCollection append / never-zero spawn)",
        "why": "Browser global, no Node/electron import (\u00a72.1 safe); collision-free; named in data-model.md. Greenfield convention, not a departure",
        "alternatives_rejected": "Monotonic module-scope counter \u2014 survives slice but not HMR cleanly; UUID is simpler + stateless"
      }
    ],
    "file_impact": [
      {
        "file": "src/renderer/src/lib/requestSpec.ts",
        "action": "Create",
        "what_changes": "RequestSpec/Row/Auth types, `isBearerAuth` guard, `makeBlankRequest()` seed factory"
      },
      {
        "file": "src/renderer/src/lib/tabsStore.ts",
        "action": "Create",
        "what_changes": "zustand slice `{ tabs, activeTabId }` + 5 actions + never-zero/neighbor/dedupe helpers"
      },
      {
        "file": "src/renderer/src/components/organisms/TabBar.tsx",
        "action": "Create",
        "what_changes": "Strip composing Tabs; store\u2192TabDescriptor mapping; label/dirty/`+` wiring"
      },
      {
        "file": "src/renderer/src/components/organisms/TabBar.css",
        "action": "Create",
        "what_changes": "Tokens-bound semantic classes; reduced-motion-gated; no inline styles"
      },
      {
        "file": "src/renderer/src/components/molecules/Tabs.tsx",
        "action": "Modify",
        "what_changes": "Opt-in `closable`/`onClose`; sibling `tabIndex={-1}` \u2715; Delete/Backspace path; `useLayoutEffect` focus restoration"
      },
      {
        "file": "src/renderer/src/components/molecules/Tabs.css",
        "action": "Modify",
        "what_changes": "Close-\u2715 styling + dirty-marker styling (token-bound)"
      },
      {
        "file": "src/renderer/src/App.tsx",
        "action": "Modify",
        "what_changes": "Inject `<Shell tabs={<TabBar />} />` at the composition root (replacing the test-fixture feeder), satisfying AC-27 while keeping Shell slot-agnostic. Shell.tsx is NOT modified and does NOT import TabBar"
      },
      {
        "file": "src/renderer/src/components/PrimitivesDemo.tsx",
        "action": "Modify",
        "what_changes": "Register a `closable` Tabs variant (and/or TabBar) for dev visual QA"
      },
      {
        "file": "src/renderer/src/lib/__tests__/tabsStore.test.ts",
        "action": "Create",
        "what_changes": "Unit tests: every action + dirty/markClean + invariants + JSON round-trip contract; INCL. two-leg dedupe (collectionRequestId then url) and `activeTabId`-unchanged-on-non-active-close"
      },
      {
        "file": "src/renderer/src/components/organisms/__tests__/TabBar.test.tsx",
        "action": "Create",
        "what_changes": "Render/select/close/new-blank + label precedence + dirty marker"
      },
      {
        "file": "src/renderer/src/components/molecules/__tests__/Tabs.test.tsx",
        "action": "Modify",
        "what_changes": "`closable=true` behavior + `closable=false` byte-identical regression"
      },
      {
        "file": "src/renderer/src/components/molecules/__tests__/Tabs.ct.tsx",
        "action": "Modify",
        "what_changes": "CT: Delete/Backspace close + roving-focus restoration + axe pass"
      }
    ],
    "doc_impact": [
      {
        "doc_file": "docs/renderer/index.md",
        "action": "Update",
        "what_changes": "Add tabsStore + requestSpec to the lib/ tree; add TabBar to organisms/; note the Tabs closable extension"
      },
      {
        "doc_file": "docs/architecture.md",
        "action": "Update",
        "what_changes": "Extend the \"Tabs \u2014 hand-rolled WAI-ARIA tablist\" pattern note with the opt-in `closable`/`onClose` contract; add the tabsStore to the State Management list; record the RequestSpec serializability contract"
      },
      {
        "doc_file": "docs/glossary.md",
        "action": "Update",
        "what_changes": "Add RequestSpec, tabsStore, TabBar terms"
      }
    ],
    "risks": [
      {
        "risk": "Per-tab \u2715 extension breaks 002 roving-tabindex a11y (nested interactive in role=tab, lost focus on close, dangling tabindex)",
        "likelihood": "High",
        "impact": "High",
        "mitigation": "Spike against the existing roving-tabindex tests BEFORE TabBar; \u2715 is sibling `tabIndex={-1}` (not a roving stop). Focus restoration: `useLayoutEffect` (NOT `useEffect`) keyed `[activeId, tabs]` + a `lastFocusWasInListRef` set via `onFocus`-capture on the tablist / cleared on `onBlur` when `relatedTarget` leaves the list \u2014 restore `buttonRefs.get(activeId)?.focus()` ONLY when the ref guard is true and `document.activeElement !== activeEl` (no mouse-user hijack; closes the focus-falls-to-`<body>` gap, AC-23). `tabIndex={0}` cannot dangle \u2014 `rovingTabStopIndex` re-derives from `(tabs, activeId)` every render. Implementer MUST NOT add \u2715 to `buttonRefs`, give \u2715 `role=tab`, or switch `handleKeyDown` to a DOM selector. Add `closable=false` byte-identical regression + `closable=true` axe pass."
      },
      {
        "risk": "Dependency-direction violation \u2014 store (lib) reaching into `components/`, or TabBar importing a sibling organism",
        "likelihood": "Med",
        "impact": "High",
        "mitigation": "`tabsStore`/`requestSpec` import nothing from `components/` (leaf lib rule); TabBar imports only the Tabs molecule + lib (downward), never another organism; enforce via `@renderer` alias + code review"
      },
      {
        "risk": "RequestSpec serialization shape drifts from the OOS persistence task",
        "likelihood": "Med",
        "impact": "Med",
        "mitigation": "Pin the Q-2 contract test now (`JSON.parse(JSON.stringify(spec))` deep-equals `spec`); keep RequestSpec plain-serializable"
      },
      {
        "risk": "Stale Vite/Playwright build cache makes a clean source file look broken (false RollupError)",
        "likelihood": "Med",
        "impact": "Med",
        "mitigation": "When a build/test error names a clean file as import source, clear the cache (`playwright/.cache`, `node_modules/.vite`, `dist`) and re-run BEFORE editing source or filing a bug"
      },
      {
        "risk": "Repo-wide reformatting/housekeeping on the feature branch pollutes the /verify hygiene scope",
        "likelihood": "Med",
        "impact": "Low",
        "mitigation": "Keep this branch's commits to the feature's src + test files; avoid `prettier --write .` and unrelated housekeeping"
      }
    ],
    "specialist_consultation": [
      {
        "specialist": "frontend-engineer",
        "sub_question": "Minimal roving-focus restoration when the focused tab is removed under signal-only `onClose`",
        "input_summary": "`useLayoutEffect` keyed `[activeId, tabs]` + a `lastFocusWasInListRef` capture-guard; restore `buttonRefs.get(activeId)?.focus()` only when guard true and not already focused; `tabIndex={0}` cannot dangle (re-derived each render); \u2715 sibling invisible to roving logic",
        "verdict": "accepted",
        "cites": "AC-11/AC-12/AC-23; Tabs.tsx roving engine (handleKeyDown ~:290, ref callback ~:373); docs/architecture.md \"Tabs \u2014 hand-rolled WAI-ARIA tablist\""
      }
    ],
    "dependencies": [
      "None. No package to install (zustand + radix-ui already declared; `crypto.randomUUID` is a browser global). No service or environment variable. The renderer test stack (Vitest + Testing Library + Playwright CT) was established by feature 001."
    ]
  }
}
